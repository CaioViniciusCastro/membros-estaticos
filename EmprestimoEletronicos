<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
<script> 
class Dispositivo{
    constructor(modelo,categoria,serial,disponivel){
        this.modelo = modelo;
        this.categoria = categoria;
        this.serial = serial;
        this.disponivel = disponivel;
    }


emprestar(){
    if (this.disponivel === true){
        this.disponivel = false;
        return "Empréstimo realizado.";
    }else if (this.disponivel === false){
        return "Empréstimo mal sucedido, produto não disponível.";
    }
}

devolver(){
    if (this.disponivel === false){
        this.disponivel = true;
    return "Produto devolvido com sucesso.";
    }else if (this.disponivel === true){
    return "Não há produtos para serem devolvidos.";
    }
}
}

class CentralDeEmprestimos extends Dispositivo{
    constructor(inventario){
    this.inventario = {
            "Modelo": this.modelo,
            "Categoria": this.categoria,
            "Serial": this.serial,
            "Disponivel": this.disponivel
        }
    this.inventario = inventario;
    }

    adicionarDispositivo(dispositivo){
        inventario.splice (0, this.dispositivo);
    }

   verificarDispositivo(){
        if (serial == serial){

        };
    }

    emprestarDispositivo(serial){
        if (disponivel in serial == true){}
    }

    devolverDispositivo(){}
}


</script>
</body>
</html>
